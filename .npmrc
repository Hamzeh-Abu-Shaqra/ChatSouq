shamefully-hoist=false
strict-peer-dependencies=false
auto-install-peers=true

# Allow these packages to run build scripts (pnpm v11 supply-chain policy)
only-built-deps[]=esbuild
only-built-deps[]=sharp
only-built-deps[]=protobufjs
